Created At: 2026-08-09T04:38:02+05:30
Completed At: 2026-08-09T04:38:13+05:30
The following changes were made by the multi_replace_file_content tool to: c:\Users\Abhix\Desktop\MY Project\Chatbot\universal-chatbot\src\components\ChatInput.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -29,300 +29,52 @@
   onSendMessage: (
     text: string, 
     attachedImage?: AttachedImage | null, 
-    mode?: "chat" | "code" | "reasoning", 
-    attachedZipContent?: string | null, 
-    zipFileName?: string | null
-  ) => void;
-  isStreaming: boolean;
-  onStopStreaming: () => void;
-  onOpenConfig: () => void;
-  hasMessages: boolean;
-  modelName: string;
-}
-
+import React, { useState, useRef, useEffect } from "react";
+import JSZip from "jszip";
+import {
+  Send,
+  Square,
+  Paperclip,
+  Sliders,
+  Sparkles,
+  ChevronDown,
+  Brain,
+  ArrowUp,
+  Cpu,
+  Layers,
+  X,
+  Image as ImageIcon,
+  Eye,
+  FileArchive,
+  FolderPlus,
+  Loader2,
+} from "lucide-react";
+
+export interface AttachedImage {
+  dataUrl: string;
+  mimeType: string;
+  fileName: string;
+}
+
+interface ChatInputProps {
+  onSendMessage: (
+    text: string, 
+    attachedImage?: AttachedImage | null, 
+    mode?: "chat" | "code" | "reasoning", 
+    attachedZipContent?: string | null, 
+    zipFileName?: string | null
+  ) => void;
+  isStreaming: boolean;
+  onStopStreaming: () => void;
+  onOpenConfig: () => void;
+  hasMessages: boolean;
+  modelName: string;
+  quotedText?: string | null;
+  onClearQuote?: () => void;
+}
+
 export const ChatInput: React.FC<ChatInputProps> = ({
   quotedText,
-  onClearQuote,
-  onSendMessage,
-  isStreaming,
-  onStopStreaming,
-  onOpenConfig,
-  hasMessages,
-  modelName,
-}) => {
-  const [input, setInput] = useState("");
-  const [mode, setMode] = useState<"chat" | "code" | "reasoning">("chat");
-  const [attachedImage, setAttachedImage] = useState<AttachedImage | null>(null);
-  const [attachedZipContent, setAttachedZipContent] = useState<string | null>(null);
-  const [zipFileName, setZipFileName] = useState<string | null>(null);
-  const [isProcessing, setIsProcessing] = useState(false);
-  const textareaRef = useRef<HTMLTextAreaElement>(null);
-  const fileInputRef = useRef<HTMLInputElement>(null);
-  const folderInputRef = useRef<HTMLInputElement>(null);
-
-  useEffect(() => {
-    if (textareaRef.current) {
-      textareaRef.current.style.height = "auto";
-      textareaRef.current.style.height = `${Math.min(
-        textareaRef.current.scrollHeight,
-        200
-      )}px`;
-    }
-  }, [input]);
-
-  const processFile = async (file: File) => {
-    if (file.name.endsWith(".zip") || file.type.includes("zip")) {
-      setIsProcessing(true);
-      try {
-        const zip = new JSZip();
-        const loadedZip = await zip.loadAsync(file);
-        let combinedText = "";
-        const allowedExtensions = [".ts", ".js", ".tsx", ".jsx", ".py", ".json", ".md", ".txt", ".html", ".css", ".java", ".cpp", ".c", ".go", ".rs", ".php", ".sh"];
-        const MAX_CHARS = 700000;
-        
-        for (const [relativePath, zipEntry] of Object.entries(loadedZip.files)) {
-          if (combinedText.length > MAX_CHARS) {
-            combinedText += `\n\n[TRUNCATED: Maximum codebase context size reached for this model]`;
-            break;
-          }
-          if (!zipEntry.dir) {
-            const ext = relativePath.substring(relativePath.lastIndexOf(".")).toLowerCase();
-            if (allowedExtensions.includes(ext) || !relativePath.includes(".")) { 
-              if (relativePath.includes("package-lock.json") || relativePath.includes("yarn.lock") || relativePath.includes("pnpm-lock.yaml")) continue;
-              
-              try {
-                const content = await zipEntry.async("text");
-                combinedText += `\n\n==== FILE: ${relativePath} ====\n${content}\n`;
-              } catch (e) {
-                // Ignore binary/unreadable
-              }
-            }
-          }
-        }
-        if (combinedText) {
-          setAttachedZipContent(combinedText);
-          setZipFileName(file.name);
-        } else {
-          alert("No readable code/text files found in the zip.");
-        }
-      } catch (err) {
-        alert("Failed to read zip file.");
-      } finally {
-        setIsProcessing(false);
-      }
-      return;
-    }
-
-    if (!file.type.startsWith("image/")) {
-      alert("Please upload an image file (PNG, JPG, WEBP) or a .zip file.");
-      return;
-    }
-    const reader = new FileReader();
-    reader.onload = (e) => {
-      if (e.target?.result) {
-        setAttachedImage({
-          dataUrl: e.target.result as string,
-          mimeType: file.type || "image/jpeg",
-          fileName: file.name || "Pasted Image",
-        });
-      }
-    };
-    reader.readAsDataURL(file);
-  };
-
-  const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
-    const file = e.target.files?.[0];
-    if (file) {
-      processFile(file);
-    }
-    if (fileInputRef.current) {
-      fileInputRef.current.value = "";
-    }
-  };
-
-  const handleFolderChange = async (e: React.ChangeEvent<HTMLInputElement>) => {
-    const files = e.target.files;
-    if (!files || files.length === 0) return;
-
-    setIsProcessing(true);
-    try {
-      let combinedText = "";
-      let fileCount = 0;
-      
-      const rootName = files[0].webkitRelativePath ? files[0].webkitRelativePath.split('/')[0] : "Folder";
-      const MAX_CHARS = 700000; // ~700KB / ~175,000 tokens (safe for 200k models)
-
-      for (let i = 0; i < files.length; i++) {
-        if (combinedText.length > MAX_CHARS) {
-          combinedText += `\n\n[TRUNCATED: Maximum codebase context size reached for this model]`;
-          break;
-        }
-
-        const file = files[i];
-        if (
-          file.name.match(/\.(png|jpe?g|gif|webp|ico|svg|bmp|mp4|webm|mp3|wav|ogg|pdf|zip|tar|gz|exe|dll|dylib|so|class|jar)$/i) ||
-          file.name.includes(".DS_Store") ||
-          file.name.includes("package-lock.json") ||
-          file.name.includes("yarn.lock") ||
-          file.name.includes("pnpm-lock.yaml") ||
-          file.webkitRelativePath.includes("node_modules/") ||
-          file.webkitRelativePath.includes(".git/") ||
-          file.webkitRelativePath.includes("vendor/") ||
-          file.webkitRelativePath.includes(".next/") ||
-          file.webkitRelativePath.includes("dist/") ||
-          file.webkitRelativePath.includes("build/") ||
-          file.webkitRelativePath.includes(".cache/")
-        ) {
-          continue;
-        }
-
-        try {
-          const text = await file.text();
-          combinedText += `\n\n--- File: ${file.webkitRelativePath} ---\n\n${text}`;
-          fileCount++;
-        } catch (err) {
-          console.warn(`Could not read ${file.webkitRelativePath}`);
-        }
-      }
-
-      if (fileCount > 0) {
-        setAttachedZipContent(combinedText.trim());
-        setZipFileName(`${rootName} (${fileCount} files)`);
-      } else {
-        alert("No readable text/code files found in the selected folder.");
-      }
-    } finally {
-      setIsProcessing(false);
-      if (folderInputRef.current) {
-        folderInputRef.current.value = "";
-      }
-    }
-  };
-
-  const handlePaste = (e: React.ClipboardEvent<HTMLTextAreaElement>) => {
-    const items = e.clipboardData?.items;
-    if (!items) return;
-    for (let i = 0; i < items.length; i++) {
-      if (items[i].type.indexOf("image") !== -1) {
-        const file = items[i].getAsFile();
-        if (file) {
-          e.preventDefault();
-          processFile(file);
-          break;
-        }
-      }
-    }
-  };
-
-  const handleKeyDown = (e: React.KeyboardEvent<HTMLTextAreaElement>) => {
-    if (e.key === "Enter" && !e.shiftKey) {
-      e.preventDefault();
-      handleSubmit();
-    }
-  };
-
-  const handleSubmit = () => {
-    if ((!input.trim() && !attachedImage && !attachedZipContent) || isStreaming) return;
-    
-    onSendMessage(input.trim(), attachedImage, mode, attachedZipContent, zipFileName);
-    
-    setInput("");
-    setAttachedImage(null);
-    setAttachedZipContent(null);
-    setZipFileName(null);
-    if (textareaRef.current) {
-      textareaRef.current.style.height = "auto";
-    }
-  };
-
-  return (
-    <div className="w-full max-w-4xl mx-auto px-4 pb-4 pt-1">
-      {/* Hidden File Input */}
-      <input
-        type="file"
-        ref={fileInputRef}
-        accept="image/*,.zip"
-        onChange={handleFileChange}
-        className="hidden"
-      />
-      {/* Hidden Folder Input */}
-      <input
-        type="file"
-        ref={folderInputRef}
-        // @ts-ignore
-        webkitdirectory=""
-        directory=""
-        multiple
-        onChange={handleFolderChange}
-        className="hidden"
-      />
-
-      {/* Floating Prompt Container */}
-      <div className="bg-app-card border border-app-border focus-within:border-app-primary focus-within:ring-1 focus-within:ring-app-primary rounded-2xl p-3.5 shadow-lg transition-all duration-200">
-        {/* Attached Zip Preview Pill if present */}
-        {zipFileName && (
-          <div className="mb-2.5 p-2 bg-app-surface border border-app-border rounded-xl flex items-center justify-between gap-3 animate-fadeIn">
-            <div className="flex items-center gap-2.5 overflow-hidden">
-              <div className="w-10 h-10 bg-app-surface-hover flex items-center justify-center rounded-lg border border-app-border shrink-0">
-                <FileArchive className="w-5 h-5 text-emerald-600" />
-              </div>
-              <div className="min-w-0">
-                <div className="text-xs font-semibold text-app-fg truncate flex items-center gap-1.5">
-                  <span className="truncate">{zipFileName}</span>
-                </div>
-                <div className="text-[10px] text-emerald-700 font-mono flex items-center gap-1 mt-0.5">
-                  <span>Codebase extracted & ready for analysis</span>
-                </div>
-              </div>
-            </div>
-            <button
-              type="button"
-              onClick={() => { setZipFileName(null); setAttachedZipContent(null); }}
-              className="p-1.5 text-app-muted hover:text-app-fg hover:text-app-fg hover:bg-app-surface-hover rounded-lg transition-colors shrink-0"
-              title="Remove Zip"
-            >
-              <X className="w-4 h-4" />
-            </button>
-          </div>
-        )}
-
-        {/* Attached Image Preview Pill if present */}
-        {attachedImage && (
-          <div className="mb-2.5 p-2 bg-app-surface border border-app-border rounded-xl flex items-center justify-between gap-3 animate-fadeIn">
-            <div className="flex items-center gap-2.5 overflow-hidden">
-              <img
-                src={attachedImage.dataUrl}
-                alt="Attachment Preview"
-                className="w-10 h-10 object-cover rounded-lg border border-app-border shrink-0"
-              />
-              <div className="min-w-0">
-                <div className="text-xs font-semibold text-app-fg truncate flex items-center gap-1.5">
-                  <ImageIcon className="w-3.5 h-3.5 text-app-primary shrink-0" />
-                  <span className="truncate">{attachedImage.fileName}</span>
-                </div>
-                <div className="text-[10px] text-indigo-700 font-mono flex items-center gap-1 mt-0.5">
-                  <Eye className="w-3 h-3 text-indigo-500 shrink-0" />
-                  <span>Auto Vision Transcoder: Converts image into text for any model</span>
-                </div>
-              </div>
-            </div>
-            <button
-              type="button"
-              onClick={() => setAttachedImage(null)}
-              className="p-1.5 text-app-muted hover:text-app-fg hover:text-app-fg hover:bg-app-surface-hover rounded-lg transition-colors shrink-0"
-              title="Remove image"
-            >
-              <X className="w-4 h-4" />
-            </button>
-          </div>
-        )}
-
-        {/* Textarea */}
-        <textarea
-          ref={textareaRef}
-          value={input}
-          onChange={(e) => setInput(e.target.value)}
-          onKeyDown={handleKeyDown}
           onPaste={handlePaste}
           placeholder={
             attachedImage
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.

We did our best to apply changes despite some inaccuracies. Double check if the edit applied is what you intended.